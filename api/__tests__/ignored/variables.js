/*
 * Variables whose value is unknown until runtime.
 */
module.exports = Object({
  /*
   * The user ID acquired during the test
   */
  userId: '',
  /*
   * The token for the user acquired during the test
   */
  token: '',
});
